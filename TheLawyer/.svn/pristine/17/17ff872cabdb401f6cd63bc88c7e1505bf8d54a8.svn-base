//
//  BaseModel.h
//  TheLawyer
//
//  Created by yaoyao on 17/7/18.
//  Copyright © 2017年 shangyukeji. All rights reserved.
//

#import <Foundation/Foundation.h>

#define PropertyString(p)         @property (nonatomic, strong) NSString *p;
#define PropertyInteger(p)        @property (nonatomic, assign) NSInteger p;
#define PropertyBool(p)           @property (nonatomic, assign) BOOL p;
#define PropertyNumber(p)         @property (nonatomic, strong) NSNumber *p;
#define PropertyNSArray(p)        @property (nonatomic, strong) NSArray *p;
#define PropertyNSMutableArray(p) @property (nonatomic, strong) NSMutableArray *p;
#define PropertyNSDictionary(p)   @property (nonatomic, strong) NSDictionary *p;
#define PropertyFloat(p)          @property (nonatomic, assign) float p;

@interface BaseModel : NSObject

//图片url
PropertyString(picUrl);
//是否执行过动画
PropertyBool(haveAnimated);



@end
